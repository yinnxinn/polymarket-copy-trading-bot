# Runtime log — leaderboard snapshots

When `LEADERBOARD_ENABLED=true`, the bot appends a markdown table here each time it resolves the Polymarket trader leaderboard (at startup and on each refresh interval, if configured).

This file is safe to delete or rotate; it is not required for trading.
